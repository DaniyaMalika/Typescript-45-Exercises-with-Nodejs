//object ka interface bnany ka keyword interface hi huta hai // object main hum always curly brackets use krty hyn
interface itCourse {
    courseName: string;
    location: string;
    totalStudents: number;
}

//ab hum object bnaengy
let itCourse = {
    courseName: "Typescript",
    location: "Governor House",
    Duration: 50000,
}
console.log(itCourse);